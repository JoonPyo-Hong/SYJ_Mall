<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">

	<select id="noticelist" resultType="com.test.SYJ_Mall.board.BoardDTO">
		SELECT
		seq,gubn, title,
		content, reg_id, CONVERT(CHAR(23),
		reg_dt, 23) as reg_dt
		FROM NOTICE
	</select>
	<select id="listbtn" resultType="com.test.SYJ_Mall.board.BoardDTO" parameterType="Integer" >
		SELECT
		seq,gubn, title,
		content, reg_id, CONVERT(CHAR(23),
		reg_dt, 23) as reg_dt
		FROM NOTICE WHERE seq = #{seq}
	</select>

</mapper>