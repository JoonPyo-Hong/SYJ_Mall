<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">
	<select id="PaySelect"
		resultType="com.test.SYJ_Mall.pay.ProductDTO" parameterType="string">
		select
		a.product_id,a.product_nm,a.product_price,b.product_img from
		KAKAO_PRODUCT_TABLE a left join KAKAO_PRODUCT_IMG b on a.product_id
		=b.product_id where a.product_id in  (${str1}) and rep_img_yn = 'Y'
	</select>
</mapper>